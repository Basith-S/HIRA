# Logs
logs
*.log
*.jsonl
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Phase 2 — Backend ──────────────────────────────────────────
# JSON incident log — keep the folder, not the data file
backend/memory/incidents.json

# ChromaDB persistent volume (Docker bind-mount)
backend/chroma_data/

# Backend node_modules and build output
backend/node_modules/
backend/dist/

# Backend secrets — never commit
backend/.env
